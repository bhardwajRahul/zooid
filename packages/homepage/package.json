{
  "name": "@zooid/homepage",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter @zooid/docs build && node --import tsx build.ts && astro build && node --import tsx postbuild.ts",
    "dev": "astro dev",
    "preview": "pnpm build && wrangler pages dev dist",
    "deploy": "pnpm build && wrangler pages deploy dist --project-name=zooid-homepage"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.1",
    "@astrojs/starlight": "^0.37.7",
    "@zooid/docs": "workspace:*",
    "@zooid/ui": "workspace:*",
    "astro": "^5.6.1",
    "sharp": "^0.34.2"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "wrangler": "^4.0.0"
  }
}
