// Re-export from @zooid/auth — this file is kept for backward compat
export {
  generateKeyPair,
  signPayload,
  verifySignature,
  exportPublicKey,
  importPrivateKey,
  importPublicKey,
  arrayBufferToBase64,
  base64ToArrayBuffer,
} from '@zooid/auth';
